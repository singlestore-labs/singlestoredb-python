
#
# This workflow depends heavily on cibuildwheels.
# https://cibuildwheel.readthedocs.io/en/stable/
#

name: Publish packages

on:
  push:
    tags:
      - 'v*-rc*'
      - 'v*-test*'
      - 'v*-alpha*'
      - 'v*-beta*'
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      publish_pypi:
        description: "Publish PyPI packages on success?"
        required: true
        type: boolean
        default: true
#     publish_anaconda:
#       description: "Publish Anaconda packages on success?"
#       required: true
#       type: boolean
#       default: true
      build_number:
        description: "Package build number"
        required: true
        type: string
        default: 0

jobs:

  setup-database:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Install dependencies
        run: |
          python --version
          python -m pip install --upgrade pip
          pip install .

      - name: Initialize database
        id: initialize-database
        run: |
          python resources/create_test_cluster.py --token="${{ secrets.CLUSTER_API_KEY }}" --project="${{ vars.CLUSTER_PROJECT }}" --init-sql singlestoredb/tests/test.sql --output=github --expires=2h "python - $GITHUB_WORKFLOW - $GITHUB_RUN_NUMBER"
        env:
          PYTHONPATH: ${{ github.workspace }}

    outputs:
      cluster-id: ${{ steps.initialize-database.outputs.cluster-id }}
      cluster-host: ${{ steps.initialize-database.outputs.cluster-host }}
      cluster-database: ${{ steps.initialize-database.outputs.cluster-database }}
      # POST /v2/clusters generates the admin password and reports it only on
      # the create response, so it travels as a job output rather than living
      # in secrets. Job outputs are not secrets: every job below re-masks it.
      cluster-password: ${{ steps.initialize-database.outputs.cluster-password }}
      cluster-password-url: ${{ steps.initialize-database.outputs.cluster-password-url }}

  build-and-test:
    needs: setup-database

    name: Build and test wheels on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os:
          - ubuntu-24.04
          - macos-14
          - windows-2022

    steps:
      # ::add-mask:: does not cross job boundaries, so the generated admin
      # password arrives here unmasked and has to be registered again before
      # any step can echo it into the log.
      - name: Mask cluster password
        env:
          CLUSTER_PASSWORD: ${{ needs.setup-database.outputs.cluster-password }}
          CLUSTER_PASSWORD_URL: ${{ needs.setup-database.outputs.cluster-password-url }}
        run: |
          echo "::add-mask::$CLUSTER_PASSWORD"
          echo "::add-mask::$CLUSTER_PASSWORD_URL"

      - uses: actions/checkout@v3

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Build sdist
        if: runner.os == 'Linux'
        run: |
          python -m build --sdist --no-isolation

      - name: Build pure Python package
        if: runner.os == 'Linux'
        run: |
          python -m build --wheel --no-isolation
        env:
          SINGLESTOREDB_BUILD_EXTENSION: "0"

      - name: Set up QEMU
        if: runner.os == 'Linux'
        uses: docker/setup-qemu-action@v2
        with:
          platforms: all

      - name: Build and test wheels
        uses: pypa/cibuildwheel@v2.16.5
        env:
          # configure cibuildwheel to build native archs ('auto'), and some
          # emulated ones
          CIBW_ARCHS_LINUX: "auto aarch64"
          CIBW_ARCHS_MACOS: "universal2"
          CIBW_BUILD: "cp39-*"
          CIBW_SKIP: "pp* *-musllinux* *-manylinux_i686"
          CIBW_TEST_COMMAND: "pytest -v --pyargs singlestoredb.tests.test_basics"
          # xdist because pyproject.toml's addopts sets -n/--dist, and PYTHONPATH
          # points --pyargs at the workspace, so that pyproject is the inifile
          # here. Without the plugin pytest exits on the unknown arguments.
          CIBW_TEST_REQUIRES: "pytest pytest-xdist"
          # cluster-password-url, not cluster-password: the generated password
          # is drawn from the full printable set, and this value has to survive
          # both the userinfo half of the URL and the single-quoted shell word
          # cibuildwheel evaluates. Percent-encoding leaves only unreserved
          # characters and %, which are inert in both.
          CIBW_ENVIRONMENT: "SINGLESTOREDB_URL='mysql://${{ secrets.CLUSTER_USER }}:${{ needs.setup-database.outputs.cluster-password-url }}@${{ needs.setup-database.outputs.cluster-host }}:3306/${{ needs.setup-database.outputs.cluster-database }}?pure_python=0'"
          PYTHONPATH: ${{ github.workspace }}

#     - name: Build conda
#       if: ${{ matrix.os != 'windows-latest' }}
#       run: |
#         # $CONDA is an environment variable pointing to the root of the miniconda directory
#         echo $CONDA/bin >> $GITHUB_PATH
#         conda update conda
#         conda install conda-build anaconda-client conda-verify
#         mkdir conda-bld
#         cd conda.recipe
#         conda build -c singlestore -c conda-forge --output-folder ../conda-bld --no-test --no-anaconda-upload .

#     - name: Build conda (Windows)
#       if: ${{ matrix.os == 'windows-latest' }}
#       run: |
#         C:\Miniconda\condabin\conda.bat update conda
#         C:\Miniconda\condabin\conda.bat install conda-build anaconda-client conda-verify
#         mkdir conda-bld
#         cd conda.recipe
#         C:\Miniconda\condabin\conda.bat build -c singlestore -c conda-forge --output-folder ../conda-bld --no-test --no-anaconda-upload .

      - name: Merge wheels and sdist
        run: |
          mkdir -p dist
          mv ./wheelhouse/*.whl ./dist/.

      - name: Archive source dist and wheel
        uses: actions/upload-artifact@v4
        with:
          name: artifacts-${{ runner.os }}
          path: dist
          retention-days: 2

#    - name: Archive conda
#       uses: actions/upload-artifact@v4
#       with:
#         name: conda-${{ matrix.os }}
#         path: ./conda-bld
#         retention-days: 2

  publish:
    needs: build-and-test

    runs-on: ubuntu-latest

    permissions:
      id-token: write   # Required for OIDC trusted publishing
      actions: read     # Required for actions/download-artifact
      contents: write   # Required for gh release create/upload

    environment:
      name: publish
      url: https://pypi.org/p/singlestoredb

    steps:
      - uses: actions/checkout@v3

      - name: Download Linux wheels and sdist
        uses: actions/download-artifact@v4
        with:
          name: artifacts-Linux
          path: dist

      - name: Download Windows wheels and sdist
        uses: actions/download-artifact@v4
        with:
          name: artifacts-Windows
          path: dist

      - name: Download Mac wheels and sdist
        uses: actions/download-artifact@v4
        with:
          name: artifacts-macOS
          path: dist

      - name: Create GitHub Release
        if: ${{ github.event_name == 'push' }}
        run: |
          gh release create ${{ github.ref_name }} dist/* --prerelease --title "${{ github.ref_name }}"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload release assets
        if: ${{ github.event_name == 'release' }}
        run: |
          gh release upload ${{ github.event.release.tag_name }} dist/* --clobber
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish to PyPI
        if: ${{ github.event_name == 'release' || github.event.inputs.publish_pypi == 'true' }}
        uses: pypa/gh-action-pypi-publish@release/v1

#     - name: Publish Conda package
#       if: ${{ github.event_name == 'release' || github.event.inputs.publish_anaconda == 'true' }}
#       env:
#         PACKAGE_BUILD_NUMBER: ${{ github.event.inputs.build_number }}
#       run: |
#         echo $CONDA/bin >> $GITHUB_PATH
#         anaconda -t "${{ secrets.ANACONDA_TOKEN }}" upload --no-progress --user SingleStore --label main conda-bld/*/singlestoredb-*.tar.bz2
#

  shutdown-database:
    needs: [setup-database, build-and-test]
    if: ${{ always() }}
    runs-on: ubuntu-latest

    steps:
      # ::add-mask:: does not cross job boundaries; see the build-and-test job.
      - name: Mask cluster password
        env:
          CLUSTER_PASSWORD: ${{ needs.setup-database.outputs.cluster-password }}
          CLUSTER_PASSWORD_URL: ${{ needs.setup-database.outputs.cluster-password-url }}
        run: |
          echo "::add-mask::$CLUSTER_PASSWORD"
          echo "::add-mask::$CLUSTER_PASSWORD_URL"

      - uses: actions/checkout@v3

      - name: Install dependencies
        run: |
          python --version
          python -m pip install --upgrade pip
          pip install .

      - name: Drop database
        if: ${{ always() }}
        # The password reaches the script through the environment rather than
        # being interpolated into the command: the generated value can contain
        # any printable character, including ones the shell would act on.
        run: |
          python resources/drop_db.py --user "$CLUSTER_USER" --password "$CLUSTER_PASSWORD" --host "$CLUSTER_HOST" --port 3306 --database "$CLUSTER_DATABASE"
        env:
          PYTHONPATH: ${{ github.workspace }}
          CLUSTER_USER: ${{ secrets.CLUSTER_USER }}
          CLUSTER_PASSWORD: ${{ needs.setup-database.outputs.cluster-password }}
          CLUSTER_HOST: ${{ needs.setup-database.outputs.cluster-host }}
          CLUSTER_DATABASE: ${{ needs.setup-database.outputs.cluster-database }}

      - name: Shutdown cluster
        if: ${{ always() }}
        run: |
          curl -H "Accept: application/json" -H "Authorization: Bearer ${{ secrets.CLUSTER_API_KEY }}" -X DELETE "https://api.singlestore.com/v2/clusters/${{ env.CLUSTER_ID }}?force=true"
        env:
          CLUSTER_ID: ${{ needs.setup-database.outputs.cluster-id }}
